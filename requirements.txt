# Core dependencies (minimal)
httpx>=0.27.0          # Async HTTP client with streaming support
anthropic>=0.25.0      # Anthropic SDK (lightweight)

# Optional: Telegram support
python-telegram-bot>=20.0  # Lightweight Telegram bot

# Optional: Discord support  
discord.py>=2.3.0     # Discord integration

# Development
pytest>=8.0.0
pytest-asyncio>=0.24.0  # Async test support
pytest-cov>=5.0.0     # Code coverage reporting
ruff>=0.3.0

# Optional extras (install separately to avoid dependency conflicts):
# - brave-search==0.1.8 (requires httpx<0.26.0, conflicts with httpx>=0.27.0)
#   Install with: pip install brave-search==0.1.8
#   Note: You may need to downgrade httpx or create a separate environment

